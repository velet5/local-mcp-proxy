{
  "identifier": "default",
  "description": "Default capabilities for MCP Hub",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-stdin-write",
    "opener:default"
  ]
}
